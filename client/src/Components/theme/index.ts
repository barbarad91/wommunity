import { createMuiTheme } from '@material-ui/core'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#bb83a7',
      light: '#eeb3d8',
      dark: '#8a5678',
    },
    secondary: {
      main: '#d7d6f0',
      light: '#fff',
      dark: '#a5a5bd',
    },
  },
})

export default theme
